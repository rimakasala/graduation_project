import 'package:flutter/material.dart';
class BorrowingInfo extends StatelessWidget {
  const BorrowingInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(

    );
  }
}
